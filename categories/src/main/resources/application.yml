spring:
  datasource:
    url: jdbc:mysql://localhost:3306/categories
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: category




  config:
    import: optional:configserver:http://localhost:8888